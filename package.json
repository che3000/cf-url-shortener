{
  "name": "cf-url-shortener",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "tailwindcss -i ./src/styles/styles.css -o ./public/styles.css --minify && echo \"export const STYLES_CSS = \\`$(cat public/styles.css)\\`;\" > src/styles/styles-inline.ts",
    "watch:css": "tailwindcss -i ./src/styles/styles.css -o ./public/styles.css --watch",
    "dev": "npm run build:css && wrangler dev",
    "deploy": "npm run build:css && wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.18",
    "typescript": "^5.9.3",
    "wrangler": "^4.56.0"
  }
}
